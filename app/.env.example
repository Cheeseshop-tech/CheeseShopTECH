# Server-side only. Nothing here is VITE_-prefixed on purpose: anything with that
# prefix is compiled into the browser bundle, where a key is readable by anyone.
SUPABASE_URL=
SUPABASE_SERVICE_KEY=
FIRECRAWL_API_KEY=
CLOUDINARY_CLOUD_NAME=
